import { MainContainer } from "../MainContainer";
import { Menu } from "../Menu";

export const Dashboard = () => {
  return (
    <MainContainer>
      <Menu />
      <div>Hello I will be a dashboard component</div>
    </MainContainer>
  );
};
